{
  "name": "lawhub-functions",
  "description": "LawHub Firebase Cloud Functions — Stripe payment backend",
  "engines": {
    "node": ">=18"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "stripe": "^14.0.0",
    "cors": "^2.8.5"
  }
}
